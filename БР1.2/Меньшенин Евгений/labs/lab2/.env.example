JWT_SECRET=dev-jwt-secret
SERVICE_TOKEN=dev-service-token
KAFKA_BOOTSTRAP_SERVERS=localhost:9092

DATABASE_USER=postgres
DATABASE_PASSWORD=postgres

# GitHub Actions deploy (images via GHCR, server only pulls):
# Required secrets: SSH_HOST, SSH_USER, SSH_PRIVATE_KEY, DEPLOY_PATH
# Optional: SSH_PORT, JWT_SECRET, SERVICE_TOKEN, POSTGRES_PASSWORD
# Optional (private GHCR packages): REGISTRY_TOKEN (PAT read:packages), REGISTRY_USER

IDENTITY_SERVICE_URL=http://localhost:8081
RESTAURANT_SERVICE_URL=http://localhost:8082
AVAILABILITY_SERVICE_URL=http://localhost:8083
BOOKING_SERVICE_URL=http://localhost:8084
FEEDBACK_SERVICE_URL=http://localhost:8085
